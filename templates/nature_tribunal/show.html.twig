{% extends 'base.html.twig' %}

{% block title %}NatureTribunal{% endblock %}

{% block body %}
    <h1>NatureTribunal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nature_tribunal.id }}</td>
            </tr>
            <tr>
                <th>Nature_nom</th>
                <td>{{ nature_tribunal.natureNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nature_tribunal_index') }}">back to list</a>

    <a href="{{ path('nature_tribunal_edit', {'id': nature_tribunal.id}) }}">edit</a>

    {{ include('nature_tribunal/_delete_form.html.twig') }}
{% endblock %}