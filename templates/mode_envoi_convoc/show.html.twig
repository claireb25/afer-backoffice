{% extends 'base.html.twig' %}

{% block title %}Mode d'envoi convocation{% endblock %}

{% block body %}
    <h1>Mode d'envoi convocation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_envoi_convoc.id }}</td>
            </tr>
            <tr>
                <th>Courrier</th>
                <td>{{ mode_envoi_convoc.courrier ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ mode_envoi_convoc.email ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mode_envoi_convoc_index') }}">Retour</a>

    <a href="{{ path('mode_envoi_convoc_edit', {'id': mode_envoi_convoc.id}) }}">Modifier</a>

    {{ include('mode_envoi_convoc/_delete_form.html.twig') }}
{% endblock %}