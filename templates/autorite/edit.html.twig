{% extends 'base.html.twig' %}

{% block title %}Edit Autorite{% endblock %}

{% block body %}
    <h1>Edit Autorite</h1>

    {{ include('autorite/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('autorite_index') }}">back to list</a>

    {{ include('autorite/_delete_form.html.twig') }}
{% endblock %}