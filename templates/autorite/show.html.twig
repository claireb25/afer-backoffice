{% extends 'base.html.twig' %}

{% block title %}Autorite{% endblock %}

{% block body %}
    <h1>Autorite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ autorite.id }}</td>
            </tr>
            <tr>
                <th>Autorite_nom</th>
                <td>{{ autorite.autoriteNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('autorite_index') }}">back to list</a>

    <a href="{{ path('autorite_edit', {'id': autorite.id}) }}">edit</a>

    {{ include('autorite/_delete_form.html.twig') }}
{% endblock %}