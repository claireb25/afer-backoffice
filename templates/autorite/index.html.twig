{% extends 'base.html.twig' %}

{% block title %}Autorite index{% endblock %}

{% block body %}
    <h1>Autorite index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Autorite_nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for autorite in autorites %}
            <tr>
                <td>{{ autorite.id }}</td>
                <td>{{ autorite.autoriteNom }}</td>
                <td>
                    <a href="{{ path('autorite_show', {'id': autorite.id}) }}">show</a>
                    <a href="{{ path('autorite_edit', {'id': autorite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('autorite_new') }}">Create new</a>
{% endblock %}