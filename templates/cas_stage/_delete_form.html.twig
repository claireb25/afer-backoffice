<form method="post" action="{{ path('cas_stage_delete', {'id': cas_stage.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cas_stage.id) }}">
    <button class="btn">Supprimer</button>
</form>