<form method="post" action="{{ path('cas_stage_delete', {'id': cas_stage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cas_stage.id) }}">
    <button class="btn">Delete</button>
</form>