{% extends 'base.html.twig' %}

{% block title %}AutoriteTribunal index{% endblock %}

{% block body %}
    <h1>AutoriteTribunal index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Autorite_nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for autorite_tribunal in autorite_tribunals %}
            <tr>
                <td>{{ autorite_tribunal.id }}</td>
                <td>{{ autorite_tribunal.autoriteNom }}</td>
                <td>
                    <a href="{{ path('autorite_tribunal_show', {'id': autorite_tribunal.id}) }}">show</a>
                    <a href="{{ path('autorite_tribunal_edit', {'id': autorite_tribunal.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('autorite_tribunal_new') }}">Create new</a>
{% endblock %}