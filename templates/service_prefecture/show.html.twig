{% extends 'base.html.twig' %}

{% block title %}ServicePrefecture{% endblock %}

{% block body %}
    <h1>ServicePrefecture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_prefecture.id }}</td>
            </tr>
            <tr>
                <th>Service_nom</th>
                <td>{{ service_prefecture.serviceNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('service_prefecture_index') }}">back to list</a>

    <a href="{{ path('service_prefecture_edit', {'id': service_prefecture.id}) }}">edit</a>

    {{ include('service_prefecture/_delete_form.html.twig') }}
{% endblock %}