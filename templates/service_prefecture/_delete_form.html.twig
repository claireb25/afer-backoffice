<form method="post" action="{{ path('service_prefecture_delete', {'id': service_prefecture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_prefecture.id) }}">
    <button class="btn">Delete</button>
</form>