{% extends 'base.html.twig' %}

{% block title %}Bordereau index{% endblock %}

{% block body %}
    <h1>Bordereau index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for bordereau in bordereaus %}
            <tr>
                <td>{{ bordereau.id }}</td>
                <td>
                    <a href="{{ path('bordereau_show', {'id': bordereau.id}) }}">show</a>
                    <a href="{{ path('bordereau_edit', {'id': bordereau.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('bordereau_new') }}">Create new</a>
{% endblock %}