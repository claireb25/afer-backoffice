{% extends 'base.html.twig' %}

{% block title %}Bordereau{% endblock %}

{% block body %}
    <h1>Bordereau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bordereau.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bordereau_index') }}">Retour</a>

    <a href="{{ path('bordereau_edit', {'id': bordereau.id}) }}">Edit</a>

    {{ include('bordereau/_delete_form.html.twig') }}
{% endblock %}