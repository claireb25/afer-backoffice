{% extends 'base.html.twig' %}

{% block title %}Bordereau{% endblock %}

{% block body %}
    <h1>Bordereau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bordereau.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bordereau_index') }}">back to list</a>

    <a href="{{ path('bordereau_edit', {'id': bordereau.id}) }}">edit</a>

    {{ include('bordereau/_delete_form.html.twig') }}
{% endblock %}