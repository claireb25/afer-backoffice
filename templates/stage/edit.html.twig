{% extends 'base.html.twig' %}

{% block title %}Modification Stage{% endblock %}

{% block body %}
    <h1>Modification Stage</h1>

    {{ include('stage/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    <a href="{{ path('stage_index') }}">
        <button class="btn">Retour</button>
    </a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}