{% extends 'base.html.twig' %}

{% block title %}Stage{% endblock %}

{% block body %}
    <h1>Stage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Stage_numero</th>
                <td>{{ stage.stageNumero }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ stage.date ? stage.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Stage_hpo</th>
                <td>{{ stage.stageHpo ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stage_index') }}">Retour</a>

    <a href="{{ path('stage_edit', {'id': stage.id}) }}">Modifier</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}