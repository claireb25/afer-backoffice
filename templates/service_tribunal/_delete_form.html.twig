<form method="post" action="{{ path('service_tribunal_delete', {'id': service_tribunal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_tribunal.id) }}">
    <button class="btn">Delete</button>
</form>