{% extends 'base.html.twig' %}

{% block title %}Nature préfecture{% endblock %}

{% block body %}
    <h1>Nature de la préfecture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ nature_prefecture.natureNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nature_prefecture_index') }}"><button>Retour</button></a>

    <a href="{{ path('nature_prefecture_edit', {'id': nature_prefecture.id}) }}"><button>Modifier</button></a>

    {{ include('nature_prefecture/_delete_form.html.twig') }}
{% endblock %}