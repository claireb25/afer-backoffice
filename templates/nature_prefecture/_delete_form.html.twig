<form method="post" action="{{ path('nature_prefecture_delete', {'id': nature_prefecture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nature_prefecture.id) }}">
    <button class="btn">Delete</button>
</form>