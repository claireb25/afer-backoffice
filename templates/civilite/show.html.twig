{% extends 'base.html.twig' %}

{% block title %}{{civilite.nom}}{% endblock %}

{% block body %}
    <h1>Civilité : {{civilite.nom}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ civilite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('civilite_index') }}"><button>Retour</button></a>

    <a href="{{ path('civilite_edit', {'id': civilite.id}) }}"><button>Modifier</button></a>


{% endblock %}