{% extends 'base.html.twig' %}

{% block title %}Civilité{% endblock %}

{% block body %}
    <h1>Civilité</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ civilite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ civilite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('civilite_index') }}">Retour</a>

    <a href="{{ path('civilite_edit', {'id': civilite.id}) }}">Edit</a>

    {{ include('civilite/_delete_form.html.twig') }}
{% endblock %}