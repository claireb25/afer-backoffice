{% extends 'base.html.twig' %}

{% block title %}ForfaitAnimateur{% endblock %}

{% block body %}
    <h1>ForfaitAnimateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forfait_animateur.id }}</td>
            </tr>
            <tr>
                <th>Forfait_prix</th>
                <td>{{ forfait_animateur.forfaitPrix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('forfait_animateur_index') }}">back to list</a>

    <a href="{{ path('forfait_animateur_edit', {'id': forfait_animateur.id}) }}">edit</a>

    {{ include('forfait_animateur/_delete_form.html.twig') }}
{% endblock %}