{% extends 'base.html.twig' %}

{% block title %}Forfait Animateur{% endblock %}

{% block body %}
    <h1>Forfait : {{ forfait_animateur.forfaitPrix}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Forfait</th>
                <td>{{ forfait_animateur.forfaitPrix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('forfait_animateur_index') }}"><button>Retour</button></a>

    <a href="{{ path('forfait_animateur_edit', {'id': forfait_animateur.id}) }}"><button>Modifier</button></a>

    {{ include('forfait_animateur/_delete_form.html.twig') }}
{% endblock %}