{% extends 'base.html.twig' %}

{% block title %}ForfaitAnimateur index{% endblock %}

{% block body %}
    <h1>ForfaitAnimateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Forfait_prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for forfait_animateur in forfait_animateurs %}
            <tr>
                <td>{{ forfait_animateur.id }}</td>
                <td>{{ forfait_animateur.forfaitPrix }}</td>
                <td>
                    <a href="{{ path('forfait_animateur_show', {'id': forfait_animateur.id}) }}">show</a>
                    <a href="{{ path('forfait_animateur_edit', {'id': forfait_animateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('forfait_animateur_new') }}">Create new</a>
{% endblock %}