{% extends 'base.html.twig' %}

{% block title %}Tribunal{% endblock %}

{% block body %}
    <h1> Tribunal : {{ tribunal.tribunalNom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nature</th>
                <td> {{ tribunal.NatureTribunalId }} </tb>
            </tr>
            <tr>
                <th>Service</th>
                <td> {{ tribunal.ServiceTribunalId }}</td>
            </tr>
             <tr>
                <th>Autorité</th>
                <td> {{ tribunal.AutoriteTribunalId }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ tribunal.adresse }}</td>
            </tr>
            <tr>
                <th>Code postal</th>
                <td>{{ tribunal.codePostal }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ tribunal.commune }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tribunal_index') }}">Retour</a>

    <a href="{{ path('tribunal_edit', {'id': tribunal.id}) }}">Modifier</a>

    {{ include('tribunal/_delete_form.html.twig') }}
{% endblock %}