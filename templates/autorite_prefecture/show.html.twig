{% extends 'base.html.twig' %}

{% block title %}{{autorite_prefecture.autoriteNom}}{% endblock %}

{% block body %}
    <h1>Autorité : {{autorite_prefecture.autoriteNom}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Autorité</th>
                <td>{{ autorite_prefecture.autoriteNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('autorite_prefecture_index') }}"><button>retour</button></a>

    <a href="{{ path('autorite_prefecture_edit', {'id': autorite_prefecture.id}) }}"><button>Modifier</button></a>

  }
{% endblock %}