{% extends 'base.html.twig' %}

{% block title %}AutoritePrefecture{% endblock %}

{% block body %}
    <h1>AutoritePrefecture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ autorite_prefecture.id }}</td>
            </tr>
            <tr>
                <th>Autorite_nom</th>
                <td>{{ autorite_prefecture.autoriteNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('autorite_prefecture_index') }}">back to list</a>

    <a href="{{ path('autorite_prefecture_edit', {'id': autorite_prefecture.id}) }}">edit</a>

    {{ include('autorite_prefecture/_delete_form.html.twig') }}
{% endblock %}