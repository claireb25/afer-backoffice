<form method="post" action="{{ path('autorite_prefecture_delete', {'id': autorite_prefecture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autorite_prefecture.id) }}">
    <button class="btn">Supprimer</button>
</form>