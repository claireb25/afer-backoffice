{% extends 'base.html.twig' %}

{% block title %}TypeInfraction{% endblock %}

{% block body %}
    <h1>TypeInfraction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_infraction.id }}</td>
            </tr>
            <tr>
                <th>Type_infraction</th>
                <td>{{ type_infraction.typeInfraction }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_infraction_index') }}">back to list</a>

    <a href="{{ path('type_infraction_edit', {'id': type_infraction.id}) }}">edit</a>

    {{ include('type_infraction/_delete_form.html.twig') }}
{% endblock %}