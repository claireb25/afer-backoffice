{% extends 'base.html.twig' %}

{% block title %}Type d'infraction{% endblock %}

{% block body %}
    <h1>Type d'infraction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_infraction.id }}</td>
            </tr>
            <tr>
                <th>Type_infraction</th>
                <td>{{ type_infraction.typeInfraction }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_infraction_index') }}">Retour</a>

    <a href="{{ path('type_infraction_edit', {'id': type_infraction.id}) }}">Modifier</a>

    {{ include('type_infraction/_delete_form.html.twig') }}
{% endblock %}