{% extends 'base.html.twig' %}

{% block title %}Permis{% endblock %}

{% block body %}
    <h1>Permis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ permi.id }}</td>
            </tr>
            <tr>
                <th>Numero_permis</th>
                <td>{{ permi.numeroPermis }}</td>
            </tr>
            <tr>
                <th>Delivre_le</th>
                <td>{{ permi.delivreLe ? permi.delivreLe|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('permis_index') }}">Retour</a>

    <a href="{{ path('permis_edit', {'id': permi.id}) }}">Modifier</a>

    {{ include('permis/_delete_form.html.twig') }}
{% endblock %}