<form method="post" action="{{ path('statut_animateur_delete', {'id': statut_animateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut_animateur.id) }}">
    <button class="btn">Delete</button>
</form>